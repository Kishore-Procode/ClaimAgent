# API client (OpenRouter is OpenAI-compatible)
openai>=1.30.0

# Environment variable loading
python-dotenv>=1.0.0

# Data
pandas>=2.0.0
pydantic>=2.0.0

# Image processing (for bonus agents — authenticity + duplicate detection)
Pillow>=10.0.0
piexif>=1.1.3
imagehash>=4.3.1

# Web server for visual dashboard
fastapi
uvicorn
websockets

